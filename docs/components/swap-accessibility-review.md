# Swap manual accessibility review

Status: Pending. No human accessibility pass is claimed.

Record tester, date, OS, browser, assistive technology, steps and results for:

- Stable checkbox/button names and native checked, pressed, mixed, disabled and read-only announcements.
- Keyboard activation, one focus stop per control, visible focus and no focusable decorative parts.
- Label pointer activation and touch; read-only guard and native disabled behavior.
- Forms validation and error descriptions; indeterminate clearing and reset.
- Light/dark contrast, forced colors, reduced motion, RTL, long labels and zoom/reflow.
- Server-rendered fallback and behavior during hydration.
